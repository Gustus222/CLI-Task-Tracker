Created a CLI Task Tracker using Java Spring Boot

Does not contain the tasks into a JSON file, but rather in a database

The database given is in-memory only for the purpose of the project

https://roadmap.sh/projects/task-tracker
